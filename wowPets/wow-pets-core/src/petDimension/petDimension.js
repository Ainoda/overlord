class petDimension {
  // pet三维
  constructor(name, code, hp, attack, speed) {
    this.name = name
    this.code = code
    this.hp = hp
    this.attack = attack
    this.speed = speed
  }
}

module.exports = petDimension
