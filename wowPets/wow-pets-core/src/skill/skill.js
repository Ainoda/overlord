class skill {
  // skill
  constructor(name, code, description, cooling, species, hitRate) {
    this.name = name
    this.code = code
    this.description = description
    this.cooling = cooling
    this.species = species
    this.hitRate = hitRate
  }
}

module.exports = skill
