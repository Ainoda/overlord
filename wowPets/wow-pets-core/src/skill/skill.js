class skill {
  // skill
  constructor(name, code, description, species, hitRate) {
    this.name = name
    this.code = code
    this.description = description
    this.species = species
    this.hitRate = hitRate
  }
}

module.exports = skill
